{
  "name": "finance-os",
  "version": "1.4.0",
  "private": true,
  "description": "Lokale, statische Finance-OS-PWA",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node --check core.js && node --check app.js && node --check service-worker.js"
  }
}
